// Creando la función
const saludar = () => console.log('Hola');

// Invocando la función
saludar()

// Uso de parámetros

const saludarUsuario = (user) => console.log(`hola ${user}`)

saludarUsuario('Fer')

const answerShipping = (priceShipping) => console.log(`El precio de tu domicilio es ${priceShipping}`)

answerShipping(`$${50000}`)

//Haciendo operaciones con funciones

const suma = (num1,num2) => num1+num2
let result = suma(3,5)
console.log(result)




